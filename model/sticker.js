/*const mongoose = require('mongoose');

const Schema = mongoose.Schema;

const stickerSchema = new Schema({
	name: String,
	count: Int32,
	changeDirection:Int32,
	max: Double
});

module.exports = mongoose.model('stiker', stickerSchema, 'sticker');*/